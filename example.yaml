esphome:
  name: "ir-module-mini-v2-3"
  friendly_name: "ir-module-mini-v2-3"

esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: esp-idf

external_components:
  - source: github://saikhurana98/esphome-ir-lg@main
    components: [lg_climate]

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "wNlQpGTwMXl1XAJarhoB8SZkNqTFqMagbaSYaYTqy0Q="

ota:
  - platform: esphome
    password: "28a65ebba29b9296ccb680a6a3c41225"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  ap:
    ssid: "Ir-Module-Mini-V2-2"
    password: "8L69tBR0D1cq"

captive_portal:

bluetooth_proxy:

remote_transmitter:
  pin: GPIO7
  carrier_duty_percent: 50%

remote_receiver:
  pin:
    number: GPIO10
    inverted: true
  dump: all
  tolerance: 25%

climate:
  - platform: lg_climate
    name: "Sai's Room - LG AC"
    # Optional timing overrides:
    # header_high: 8000
    # header_low: 4000
    # bit_high: 600
    # bit_one_low: 1600
    # bit_zero_low: 550
